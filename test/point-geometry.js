import test from 'node:test';
import pointGeometry from '../lib/point-geometry.js';

test.todo('point-geometry must have at least one test', t => {
  pointGeometry();
  t.assert.fail('Need to write tests.');
});
